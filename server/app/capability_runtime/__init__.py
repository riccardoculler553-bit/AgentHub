"""Capability Runtime (V1.4): enterprise automation capability domain.

AutomationCapability = WHAT the business can do (e.g. amazon.order.download).
Deliberately separate from app.capability (DeviceCapability = WHAT a device
environment has, e.g. Windows/Chrome/影刀). The two must never be conflated
(V1.4 §23).
"""
