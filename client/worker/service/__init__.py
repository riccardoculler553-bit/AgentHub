"""Worker-plane alias for the service wrapper (V1.7 doc §21).

Canonical module: client/service/windows_service.py. This package exists so
the flat worker layout also exposes it as worker.service.windows_service.
"""
